<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Google+Sans:ital,opsz,wght@0,17..18,400..700;1,17..18,400..700&display=swap"
    rel="stylesheet">
</head>

<body>
  <header>
    <%= title %>
      <a href="/create" class="header-button">Create User</a>
  </header>
  <% if (locals.users) { %>
    <div class="user-area">
      <% users.forEach(user=> { %>
        <div class="user">
          <h1>
            <%= user.getFirstName() %>
          </h1>
          <div class="details">
            <p>
              ID: <%= user.getID() %>
            </p>
            <p>
              First Name: <%= user.getFirstName() %>
            </p>
            <p>
              Last Name: <%= user.getLastName() %>
            </p>
          </div>
          <div class="buttons">
            <button><a href="/<%= user.getID() %>/update">Update</a></button>
            <form action="/<%= user.getID() %>/delete" method="post"><button type="submit"
                onclick="return confirm('Are you sure you want to delete this user?')">Delete User</button></form>
          </div>
        </div>
        <% }); %>
    </div>
    <% } %>
</body>

</html>