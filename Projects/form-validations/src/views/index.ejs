<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Google+Sans:ital,opsz,wght@0,17..18,400..700;1,17..18,400..700&display=swap"
    rel="stylesheet">
</head>

<body>
  <header>
    <%= title %>
      <button class="header-button"><a href="/create">Create User</a></button>
  </header>
  <% if (locals.users) { %>
    <div class="user-area">
      <% users.forEach(user=> { %>
        <div class="user">
          <h1>
            <%= user.firstName %>
          </h1>
          <div class="details">
            <p>
              ID: <%= user.id %>
            </p>
            <p>
              First Name: <%= user.firstName %>
            </p>
            <p>
              Last Name: <%= user.lastName %>
            </p>
          </div>
          <div class="buttons">
            <button><a href="/<%= user.id %>/update">Update</a></button>
            <form action="/<%= user.id %>/delete"><button type="submit"
                onclick="return confirm('Are you sure you want to delete this user?')">Submit</button></form>
          </div>
        </div>
        <% }); %>
    </div>
    <% } %>
</body>

</html>